name: Build Windows EXE

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install deps
        run: |
          python -m pip install numpy matplotlib

      - name: Clone matplotlib-cpp
        run: git clone https://github.com/lava/matplotlib-cpp.git

      - name: Build executable
        shell: bash
        run: |
          PY_INC=$(python -c "import sysconfig; print(sysconfig.get_paths()['include'])")
          PY_LIB=$(python -c "import sysconfig; print(sysconfig.get_config_var('LIBDIR'))")
          PY_VER=$(python -c "import sys; print(f'{sys.version_info.major}{sys.version_info.minor}')")

          echo "Python include path: $PY_INC"
          echo "Python lib path: $PY_LIB"
          echo "Python version ID: $PY_VER"

          g++ LancerDePoids.cpp -std=c++17 \
          -I./matplotlib-cpp \
          -I"$(python -c 'import numpy; print(numpy.get_include())')" \
          -I"$PY_INC" -L"$PY_LIB" -lpython$PY_VER \
          -o LancerDePoids.exe

      - name: Upload executable
        uses: actions/upload-artifact@v4
        with:
          name: LancerDePoids.exe
          path: LancerDePoids.exe
